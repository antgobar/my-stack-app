---
description: FastAPI, SQLModel, and Alembic patterns
globs: **/*.py
alwaysApply: false
---

# FastAPI Patterns

## Router Structure
```python
# app/routers/items.py
from fastapi import APIRouter, Depends, HTTPException
from sqlmodel import Session, select
from app.database import get_session
from app.models.item import Item

router = APIRouter(prefix="/items", tags=["items"])

@router.get("/", response_model=list[Item])
async def list_items(session: Session = Depends(get_session)):
    return session.exec(select(Item)).all()
```

## SQLModel Models
```python
# app/models/item.py
from sqlmodel import SQLModel, Field
from typing import Optional

class ItemBase(SQLModel):
    name: str
    description: Optional[str] = None

class Item(ItemBase, table=True):
    id: Optional[int] = Field(default=None, primary_key=True)

class ItemCreate(ItemBase):
    pass
```
- `table=True` → DB table; base class → Pydantic schema
- Never use `table=True` on base/create/read models

## Dependency Injection
```python
# app/database.py
from sqlmodel import Session, create_engine
from app.config import settings

engine = create_engine(settings.database_url)

def get_session():
    with Session(engine) as session:
        yield session
```

## Error Handling
```python
# ❌ BAD
return {"error": "not found"}

# ✅ GOOD
raise HTTPException(status_code=404, detail="Item not found")
```

## Alembic Migrations
- After changing models: `uv run alembic revision --autogenerate -m "description"`
- Apply: `uv run alembic upgrade head`
- Never edit existing migration files; always create new ones

## Package Management (uv)
- Add a dependency: `uv add <package>`
- Add a dev dependency: `uv add --dev <package>`
- Run any tool: `uv run <command>` (no need to activate venv)
- Sync after pulling: `uv sync`
